# core-service — clients, properties, inspections, inspection areas, dashboard stats

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
email-validator>=2.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# JWT decode (shared auth_deps)
PyJWT>=2.8.0

# Password hashing (shared password module — used by seed_demo)
pwdlib[argon2]>=0.6.0

# Cross-service HTTP calls (integration day)
httpx>=0.27.0

# Config / env
python-dotenv>=1.0.0

# Render Blueprint Configuration for DefectLoupe
# Docs: https://render.com/docs/blueprint-spec

services:
  # ----------------------------------------------------------------------------
  # 1. Frontend Web App (Static Site)
  # ----------------------------------------------------------------------------
  - type: web
    name: defectloupe-web
    runtime: static
    buildCommand: cd web && npm install && npm run build
    staticPublishPath: web/dist
    envVars:
      - key: VITE_API_BASE_URL
        sync: false

  # ----------------------------------------------------------------------------
  # 2. Auth Microservice
  # ----------------------------------------------------------------------------
  - type: web
    name: defectloupe-auth
    env: docker
    dockerContext: .
    dockerfilePath: services/auth/Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ACCESS_TOKEN_SECRET
        generateValue: true
      - key: REFRESH_TOKEN_SECRET
        generateValue: true
      - key: AUTO_VERIFY_EMAIL
        value: "true"
      - key: COOKIE_SECURE
        value: "false"

  # ----------------------------------------------------------------------------
  # 3. Core Microservice
  # ----------------------------------------------------------------------------
  - type: web
    name: defectloupe-core
    env: docker
    dockerContext: .
    dockerfilePath: services/core/Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ACCESS_TOKEN_SECRET
        fromService:
          type: web
          name: defectloupe-auth
          property: envVar:ACCESS_TOKEN_SECRET

  # ----------------------------------------------------------------------------
  # 4. Media Microservice
  # ----------------------------------------------------------------------------
  - type: web
    name: defectloupe-media
    env: docker
    dockerContext: .
    dockerfilePath: services/media/Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: ACCESS_TOKEN_SECRET
        fromService:
          type: web
          name: defectloupe-auth
          property: envVar:ACCESS_TOKEN_SECRET
      - key: UPLOAD_DIR
        value: /data/uploads

  # ----------------------------------------------------------------------------
  # 5. AI & RAG Microservice
  # ----------------------------------------------------------------------------
  - type: web
    name: defectloupe-ai
    env: docker
    dockerContext: .
    dockerfilePath: services/ai/Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: ACCESS_TOKEN_SECRET
        fromService:
          type: web
          name: defectloupe-auth
          property: envVar:ACCESS_TOKEN_SECRET
      - key: GEMINI_API_KEY
        sync: false
      - key: GEMINI_MODEL
        value: gemini-2.5-flash

  # ----------------------------------------------------------------------------
  # 6. Celery Async Worker
  # ----------------------------------------------------------------------------
  - type: worker
    name: defectloupe-celery
    env: docker
    dockerContext: .
    dockerfilePath: services/ai/Dockerfile
    dockerCommand: celery -A app.workers.celery_app worker -l info -Q celery,default,stt,vision,reports --concurrency=2
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: ACCESS_TOKEN_SECRET
        fromService:
          type: web
          name: defectloupe-auth
          property: envVar:ACCESS_TOKEN_SECRET
      - key: GEMINI_API_KEY
        sync: false
